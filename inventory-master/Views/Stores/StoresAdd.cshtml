@model vmStores
@using System.ComponentModel.DataAnnotations;
@using InventorySystem.Data.Enum;

@section body

    {
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                                 @if(@ViewData["AlertMessage"] !=null){
            @if (@ViewData["Status"] == "Success")
            {
              <div class="alert alert-success alert-dismissible fade show" role="alert">
                          @ViewData["AlertMessage"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
            }
            @if (@ViewData["Status"] == "Error")
            {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          @ViewData["AlertMessage"]
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
            }

        }
                    <div class="card card-primary">
                        <div class="card-header">
                            <h3 class="card-title">Add Store </h3>
                        </div>
                        <!-- /.card-header -->
                        <!-- form start -->
                        <form class="form-horizontal" method="post">
                            <div class="card-body">
                                <input type="hidden" asp-for="Store.Id">
                                <div class="form-group row">
                                    <label for="inputEmail3" class="col-sm-2 col-form-label"> Name</label>
                                    <div class="col-sm-10">
                                        <input type="text" class="form-control" placeholder=" Name" asp-for="Store.Name">
                                    </div>
                                </div>

                                <div class="form-group row">
                                    <label for="inputPassword3" class="col-sm-2 col-form-label">Address</label>
                                    <div class="col-sm-10">
                                        <input type="text" class="form-control" placeholder="Address" asp-for="Store.Address">
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label for="inputPassword3" class="col-sm-2 col-form-label">Phone</label>
                                    <div class="col-sm-10">
                                        <input type="text" class="form-control" placeholder="Phone" asp-for="Store.phone">
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label for="inputEmail3" class="col-sm-2 col-form-label">Company</label>
                                    <div class="col-sm-10">
                                        <select class="form-control"  asp-for="Store.Company_Id" asp-items='new SelectList(Model.LiCompany,"Id","Name")'>
                                            <option>Select Company</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label for="inputEmail3" class="col-sm-2 col-form-label">Status</label>
                                    <div class="col-sm-10">
                                        <select class="form-control" asp-for="Store.Status" asp-items="Html.GetEnumSelectList<StatusEnum>() ">
                                            <option>Select Status</option>

                                        </select>
                                    </div>
                                </div>

                            </div>
                           
                            <div class="card-footer">
                                @if ((bool)ViewData["IsEdit"] == true)
                                {
                                    <input type="submit" class="btn btn-primary" asp-action="Update" value="Update" />
                                }
                                else
                                {
                                    <input type="submit" class="btn btn-primary" asp-action="Save" value="Save" />
                                }
                                <input type="submit" class="btn btn-danger" asp-action="Index" value="Cancel" />
                            </div>
                            <!-- /.card-footer -->
                        </form>
                    </div>
                </div>
            </div>
            <!-- /.row -->
        </div><!-- /.container-fluid -->
    </section>
    <!-- /.content -->
}